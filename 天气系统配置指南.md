# 天气系统跨场景配置指南

## 🔴 问题诊断

### 问题1: RainVFX引用为空
**原因**: WeatherManager是`DontDestroyOnLoad`跨场景单例，但RainVFX是场景对象，场景切换时会被销毁。

### 问题2: 相机找到的是MainCamera而不是FPV
**原因**: RainVFXFollowCamera默认查找`Camera.main`（带MainCamera tag的相机），你的FPV相机可能没有这个tag。

---

## ✅ 解决方案（两种方法）

### 方法一：推荐 - 将RainVFX作为WeatherManager的子对象

#### 步骤：
1. **在TaskSelect场景的Hierarchy中**：
   ```
   WeatherManager (GameObject)
   └── RainVFX (VisualEffect子对象)
   ```
   - 直接在Hierarchy中把RainVFX拖动到WeatherManager下面

2. **配置WeatherManager**：
   - 选中WeatherManager
   - 把子对象RainVFX拖入`Rain VFX`字段

3. **配置RainVFX组件**：
   - 选中RainVFX子对象
   - 添加组件：`RainVFXFollowCamera`
   - 设置参数：
     - Height Offset: `20`
     - Smooth Follow: `✓`
     - Follow Speed: `5`
     - Rain Area Size: `(50, 5, 50)`

4. **检查日志**：
   运行TaskSelect场景，应该看到：
   ```
   [WeatherManager] 已启动并设为跨场景持久化
   [WeatherManager] RainVFX已连接: RainVFX
   [RainVFXFollowCamera] 雨VFX将跟随相机: fpvCamera (Tag: Untagged)
   ```

#### 这样做的好处：
- ✅ RainVFX会自动跟随WeatherManager跨场景保留
- ✅ 不会出现引用丢失问题
- ✅ 只需配置一次，所有场景都有效

---

### 方法二：在每个场景都放置RainVFX

如果你希望每个场景独立控制雨效果：

1. **在TaskSelect场景**：
   - WeatherManager单独存在（不作为DontDestroyOnLoad）
   - RainVFX独立存在
   - 正常配置引用

2. **在游戏场景（OutdoorsScene等）**：
   - 不需要创建新的WeatherManager（会自动从TaskSelect保留）
   - 需要在每个场景创建新的RainVFX对象
   - 添加代码动态查找并连接：

```csharp
// 在WeatherManager的Start()中添加
private void Start()
{
    // 如果引用为空，尝试在当前场景查找
    if (rainVFX == null)
    {
        GameObject rainObj = GameObject.Find("RainVFX");
        if (rainObj != null)
        {
            rainVFX = rainObj.GetComponent<VisualEffect>();
            Debug.Log($"<color=cyan>[WeatherManager]</color> 动态找到场景中的RainVFX");
        }
    }
    
    // 验证引用...
}
```

❌ **不推荐此方法**：需要在每个场景重复配置，容易出错。

---

## 🎥 相机配置

### FPV相机设置

1. **找到你的FPV相机**（可能叫`fpvCamera`或`GimbalCamera`）

2. **设置MainCamera tag**（推荐）：
   - 选中FPV相机
   - 在Inspector顶部的Tag下拉菜单
   - 选择`MainCamera`
   - 这样`Camera.main`就会找到FPV相机

3. **或者手动指定**：
   - 选中RainVFX对象
   - 找到`RainVFXFollowCamera`组件
   - 把FPV相机拖入`Target Camera`字段

### 相机查找优先级（已修复）
RainVFXFollowCamera现在会按以下顺序查找：
1. 手动指定的`targetCamera`
2. `Camera.main`（带MainCamera tag的相机）
3. 查找名为"fpvCamera"的GameObject
4. 使用第一个找到的激活相机

日志会显示实际使用的相机名称和tag。

---

## 🧪 测试步骤

### 1. 在TaskSelect场景测试
运行后观察Console日志：
```
✅ [WeatherManager] 已启动并设为跨场景持久化
✅ [WeatherManager] RainVFX已连接: RainVFX
✅ [RainVFXFollowCamera] 雨VFX将跟随相机: fpvCamera (Tag: Untagged)
```

如果看到：
```
❌ [WeatherManager] RainVFX引用为空！
```
说明引用丢失，需要将RainVFX设为子对象。

---

### 2. 切换到游戏场景测试
从TaskSelect进入游戏场景后：

**观察Hierarchy**：
- 应该看到`DontDestroyOnLoad`文件夹
- 里面有WeatherManager（带RainVFX子对象）

**观察日志**：
- 不应该看到"检测到重复实例"（说明单例工作正常）
- RainVFX应该持续跟随相机

**观察游戏画面**：
- 雨应该始终在相机上方，不会停留在原点
- 移动相机，雨区域会跟随

---

### 3. 调整WeatherManager参数测试

在运行时修改参数（Inspector中勾选Debug模式可以实时调整）：

```csharp
// 在Console中执行或通过Inspector调整
WeatherManager.Instance.rainIntensity = 0.8f;  // 增加雨量
WeatherManager.Instance.baseWindSpeed = 15f;   // 增加风速
WeatherManager.Instance.windDirection = 90f;   // 改变风向
```

应该看到：
- VFX Graph的SpawnRate增加
- 雨滴倾斜角度改变（WindVelocity）

---

## 📋 完整配置清单

### TaskSelect场景结构
```
TaskSelect (Scene)
├── WeatherManager (GameObject)
│   ├── Component: WeatherManager.cs
│   │   ├── Rain VFX: → RainVFX (子对象)
│   │   ├── Base Wind Speed: 5
│   │   ├── Wind Direction: 0
│   │   └── Rain Intensity: 0.5
│   └── RainVFX (VisualEffect子对象)
│       ├── Component: Visual Effect
│       │   └── Asset: Rain.vfx
│       └── Component: RainVFXFollowCamera.cs
│           ├── Target Camera: [可选] 拖入FPV相机
│           ├── Height Offset: 20
│           ├── Smooth Follow: ✓
│           └── Follow Speed: 5
```

### VFX Graph配置
确保Rain.vfx中暴露了以下属性：
- `SpawnRate` (Float)
- `WindVelocity` (Vector3)
- `RainColor` (Vector4)

在VFX Graph Inspector中：
- Exposed Properties应该显示这些参数
- 可以在运行时通过代码修改

---

## 🐛 常见问题排查

### Q1: "RainVFX引用为空"
**A**: RainVFX不是WeatherManager的子对象，被场景切换销毁了。
**解决**: 在Hierarchy中将RainVFX拖到WeatherManager下。

### Q2: "雨固定在世界原点不动"
**A**: RainVFXFollowCamera组件没有生效。
**解决**: 
- 检查RainVFX上是否挂载了`RainVFXFollowCamera`组件
- 检查日志是否找到了相机

### Q3: "相机找到的不是FPV"
**A**: FPV相机没有MainCamera tag。
**解决**: 
- 给FPV相机添加MainCamera tag
- 或手动指定Target Camera

### Q4: "雨跟随很卡顿"
**A**: Smooth Follow速度太慢或VFX性能问题。
**解决**:
- 增加Follow Speed (默认5，可尝试10-20)
- 或关闭Smooth Follow直接跟随
- 检查VFX Graph性能设置

### Q5: "场景切换后雨消失了"
**A**: WeatherManager可能被重复创建并销毁。
**解决**: 
- 检查其他场景是否也有WeatherManager对象
- 应该只在TaskSelect场景有一个WeatherManager
- 其他场景会自动继承

---

## 🎯 验证成功的标志

### 日志正常输出：
```
[WeatherManager] 已启动并设为跨场景持久化
[WeatherManager] RainVFX已连接: RainVFX
[RainVFXFollowCamera] 雨VFX将跟随相机: fpvCamera (Tag: Untagged)
```

### Hierarchy结构正确：
```
DontDestroyOnLoad
└── WeatherManager
    └── RainVFX
```

### 视觉效果正常：
- ✅ 雨始终在相机上方
- ✅ 移动相机，雨区域跟随
- ✅ 场景切换后雨仍然存在
- ✅ 调整rainIntensity参数，雨量实时改变

---

## 💡 优化建议

### 性能优化
1. 调整VFX Graph的Capacity（当前500000，可以降低到50000）
2. 使用Culling Distance限制远处雨滴
3. 降低Height Offset（20m→10m）减小渲染区域

### 视觉优化
1. 调整Rain Visual Wind Scale（0.5→1.0）增加风吹效果
2. 配置Rain Color的Alpha通道控制透明度
3. 添加Fog VFX增强氛围

### 代码优化
1. 考虑添加天气预设系统（晴天/小雨/暴雨/大风）
2. 实现天气渐变过渡（lerp intensity）
3. 添加昼夜循环影响天气效果
